From c0e4b71dc6a7bc39e9e242a35f188f05a3130dd6 Mon Sep 17 00:00:00 2001
From: KRT <krt@local>
Date: Mon, 4 May 2026 15:01:06 -0400
Subject: [PATCH 2/2] Allow extensionsGallery in product.json (open-vsx)

---
 build/hygiene.ts | 8 +-------
 1 file changed, 1 insertion(+), 7 deletions(-)

diff --git a/build/hygiene.ts b/build/hygiene.ts
index 8dd2927cabe..0544879e14f 100644
--- a/build/hygiene.ts
+++ b/build/hygiene.ts
@@ -49,14 +49,8 @@ export function hygiene(some: NodeJS.ReadWriteStream | string[] | undefined, run
 	console.log('Starting hygiene...');
 	let errorCount = 0;
 
+	// KRT: extensionsGallery hygiene check removed - KRT ships an open-vsx gallery URL. See PLAN.md Phase 1.
 	const productJson = es.through(function (file: VinylFile) {
-		const product = JSON.parse(file.contents!.toString('utf8'));
-
-		if (product.extensionsGallery) {
-			console.error(`product.json: Contains 'extensionsGallery'`);
-			errorCount++;
-		}
-
 		this.emit('data', file);
 	});
 
-- 
2.52.0

